name: Extract zip

on: [push, workflow_dispatch]

jobs:
  extract:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          node-version: '24'
      - name: Giải nén duckdz.zip
        run: unzip -o duckdz.zip
